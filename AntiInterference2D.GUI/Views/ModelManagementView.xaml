<UserControl x:Class="AntiInterference2D.GUI.Views.ModelManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="模型管理" 
                   Style="{StaticResource TitleTextStyle}" Margin="8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8">
            <Button Content="🔄 刷新列表" 
                    Command="{Binding RefreshModelsCommand}"
                    Style="{StaticResource PrimaryButtonStyle}" Margin="4"/>
            <Button Content="✅ 切换选中模型" 
                    Command="{Binding SwitchModelCommand}"
                    Style="{StaticResource PrimaryButtonStyle}" Margin="4"
                    Background="{StaticResource SuccessBrush}"/>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="8">
            <DataGrid ItemsSource="{Binding Models}" 
                      SelectedItem="{Binding SelectedModel}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="模型名称" Binding="{Binding Name}" Width="200"/>
                    <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="100"/>
                    <DataGridTextColumn Header="大小 (MB)" Binding="{Binding SizeMb}" Width="100"/>
                    <DataGridCheckBoxColumn Header="已加载" Binding="{Binding Loaded}" Width="80"/>
                    <DataGridTextColumn Header="路径" Binding="{Binding Path}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" 
                   Margin="8" FontWeight="SemiBold"/>
    </Grid>
</UserControl>
