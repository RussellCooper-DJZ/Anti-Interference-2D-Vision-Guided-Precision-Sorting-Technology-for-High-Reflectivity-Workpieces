# 高反光工件抗干扰 2D 视觉引导分拣硬件与光学方案设计

## 1. 方案概述

本方案针对高反光金属工件（如不锈钢、铝合金、电镀件）在工业环境下的精准分拣需求，充分利用瑞萨 **RA8P1** 内置的 **Simple ISP** 功能，通过低成本硬件组合实现高性能视觉识别。核心思路是利用 Simple ISP 处理低价 CMOS 传感器的 Bayer RAW 数据，结合多重曝光 HDR 技术与偏振光学设计，消除镜面反射干扰。

## 2. 硬件选型清单

| 组件类别 | 推荐型号/规格 | 选型理由 |
| :--- | :--- | :--- |
| **核心控制器** | **Renesas RA8P1** (Cortex-M85) | 内置 **Simple ISP**，支持 RAW8/10/12 输入；Helium 加速指令集提升 AI 推理速度；低功耗、高集成度。 |
| **图像传感器** | **OmniVision OV5640** 或 **Sony IMX 系列** (RAW 输出) | 支持 Bayer RGB RAW 输出，完美匹配 Simple ISP；5MP 分辨率满足精度要求；成本极低。 |
| **镜头** | 12mm/16mm 工业定焦镜头 (低畸变) | 根据工作距离选型，确保工件在视野中占据足够像素；低畸变有利于亚像素定位。 |
| **偏振片** | 线性偏振片 (安装于镜头前) | 与偏振光源配合，通过正交偏振原理消除 90% 以上的镜面反射光。 |
| **光源** | **偏振条形光源** 或 **偏振环形光源** | 500-1000 Lux 亮度；内置偏振膜，减少环境光干扰并抑制工件表面光斑。 |
| **机器人接口** | UART / CAN / Ethernet | RA8P1 丰富的接口可直接与主流工业机器人（如库卡、发那科）通信。 |

## 3. 光学方案设计

### 3.1 偏振成像原理 (Polarized Imaging)
高反光工件表面的镜面反射光具有很强的偏振特性。通过在光源前加装偏振片，并在镜头前加装与之正交（旋转 90°）的偏振片，可以有效过滤掉直接反射的强光，仅保留携带工件表面纹理信息的漫反射光。

### 3.2 多重曝光 HDR 策略
利用 RA8P1 控制传感器进行快速的三重曝光（欠曝、正常、过曝）：
1.  **欠曝 (Under-exposure)**：捕捉高反光区域的边缘细节，防止过曝导致特征丢失。
2.  **正常曝光 (Normal)**：获取背景和普通区域的清晰图像。
3.  **过曝 (Over-exposure)**：提取暗部区域（如油污、指纹干扰处）的特征。
通过 Simple ISP 预处理后，由软件算法进行融合，生成高动态范围图像。

## 4. Simple ISP 参数调优策略

针对高反光场景，建议在 RA8P1 中通过 V4L2 接口进行如下参数设置：

| 参数 ID | 建议设置 | 目的 |
| :--- | :--- | :--- |
| `V4L2_CID_RZ_ISP_GAMMA` | 150 - 200 (1.5 - 2.0) | 提升暗部对比度，压制高光。 |
| `V4L2_CID_RZ_ISP_2DNR` | 70 - 100 | 消除传感器在高增益下的平面噪声。 |
| `V4L2_CID_RZ_ISP_EMP` | 2 (正常) | 强化工件边缘，提升亚像素定位精度。 |
| `V4L2_CID_RZ_ISP_BL` | 10 - 20 | 适当提升黑电平，过滤背景微弱反光。 |

## 5. 成本优势分析
*   **无需外置 ISP**：节省约 $5-$10 的硬件成本。
*   **低价传感器**：支持市面通用的 Bayer RAW 传感器，相比集成 ISP 的传感器成本降低 30% 以上。
*   **高集成度**：RA8P1 单芯片完成图像采集、ISP 处理、AI 推理与运动控制，简化电路设计。
